<h2>Home</h2>
<ul>
  {{#each wombles}}
    <li data-details="{{showingDetails}}" data-name="{{name}}">
      <span>{{name}} ({{email}})</span>
      {{#if showingDetails}}
        {{> details}}
      {{/if}}
    </li>
  {{/each}}
</ul>
{{> footer}}
